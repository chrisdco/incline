# Incline — two-week plan (18 Aug – 31 Aug 2026)

Human reference for proving the last ship, then one product slice. Small agents: read **Agent slices** at the bottom and take **one** slice per session.

**North star:** offline coach that is trustworthy. Do not start P3 (friends, Health, marketplace) or P4 (#83 GC / SyncClient).

**Current branch:** `feat/sync-fidelity` → [PR #127](https://github.com/chrisdco/incline/pull/127)
**Schema:** local 16. Cloud still needs the idempotent `supabase/sync-schema.sql` re-run.

Related: [HANDOFF.md](./HANDOFF.md) (live state) · [P1-P2-COACHING.md](./P1-P2-COACHING.md) · [ROADMAP.md](../ROADMAP.md)

---

## Constraints (do not break)

- Clerk JWT template `supabase`; never reintroduce Supabase Auth
- SQLite + outbox is source of truth; coaching is recomputed, not synced
- Session complete never awaits network / AI / photo upload
- Photo `uri` stays a local file path
- Seed program/template ids are integers, never per-device UUIDs
- Reminders stay device-local

---

## Week 1 — Close P0 ops (non-negotiable)

Goal: a second phone can restore workouts, programs, prefs, and photos.

### Sprint 1.1 — Land the branch

- [ ] CI green on #127
- [ ] Merge #127 to `main`
- [ ] Confirm schema 16 on a fresh install and an upgrade from a pre-16 DB

### Sprint 1.2 — Deploy cloud

- [ ] Re-run full `supabase/sync-schema.sql` (idempotent: `DROP POLICY IF EXISTS`)
- [ ] Confirm tables: `body_measurements`, `user_programs`, `user_program_workouts`, `user_active_program`, `user_preferences`, `workout_photos`
- [ ] Confirm private Storage bucket `workout-photos`
- [ ] Confirm live `set_entries` has `set_type` / `rpe` / `superset_group`
- [ ] Deploy #99: `supabase/coach-narrate.sql` + Edge Function + `OPENAI_API_KEY` (keep `aiExplanationsEnabled` default off)

### Sprint 1.3 — Two-device matrix (close issues only after this)

Device A logs → sync → Device B (fresh install, same Clerk user) pulls.

| Area | Issue | Pass if |
|------|--------|---------|
| Sets: RPE, set type, supersets | #119 / #57 | B shows the same sets |
| Circumference | #120 | Measures match |
| Custom program + active program (custom UUID **or** seed id) | #121 | B follows the same week/day |
| Account prefs (theme, rest defaults, ghost). Not haptics/reminders | #122 | Theme/rest match; reminders stay per device |
| Last time (working-set volume) | #38 | Same “Last time” numbers; extra warm-ups do not inflate |
| Session photos restore | #123 / #124 / #109 | B downloads JPEGs; compare still uses local files |

**Close after pass:** #38 with the PR. #57 and #109 only after the matrix.
**Do not close on merge alone.**

Buffer: whatever the matrix finds (RLS, blocked pull cursor, photo queue, account-switch wipe). That work outranks new features.

---

## Week 2 — Pick exactly one track

### Track A — Session alerts (recommended if gym use is daily)

Small, optional, high gym payoff. Default **off** or gated by existing Rest timer alerts.

- [ ] Rest remaining while locked: iOS Live Activity (`expo-widgets`) **or** Android ongoing notification
- [ ] Abandoned session: one local alert if a log is still open ~20–30 min with no new sets; tap → that session
- [ ] Rest/reminder tap currently goes to Home — point rest-complete at the live session if one exists

**Out of scope:** log-set from a notification, extra “come train” nags, monthly recap, home-screen widgets.

Needs a **new native build**. Not Expo Go.

### Track B — Analytics honesty (#126)

If “Last time” vs Progress volume already feels wrong.

- [ ] Decide stored `total_volume` vs working-set volume (do not silently change history)
- [ ] Align Progress / recap / ghost with one definition
- [ ] Tests so warm-ups cannot win volume

### Track C — AI narrations live (#99 polish)

Only if Week 1 deploy already works with a real key.

- [ ] Turn on AI explanations on a test account
- [ ] Confirm Home/summary wording never invents loads
- [ ] Rate limit + stub path still safe when the key is missing

### Do not pick for Week 2

| Item | Why later |
|------|-----------|
| Home-screen widgets | Native rebuild, iOS-only in Expo 57, no gym-loop value until rest-on-lock exists |
| #94 measurement goals | Wait until Measures is used regularly |
| #112 native Google sheet | Auth polish, not the logger |
| #78 friends / public + #125 signed URLs | P3; photos stay private |
| #83 GC / field LWW / SyncClient | P4 until multi-device is boring |
| Chat coach / program generation | Out of scope; no model keys in the app |

---

## Backlog after these two weeks (not this sprint)

**P0 leftovers / hygiene**

- E2E for onboard → log → finish → progress (ROADMAP: after P0 stabilizes)
- README schema version still says v11 — docs drift (see Agent slices)
- Monthly recap notification (payload already exists; optional like Sunday digest)

**P2 leftovers**

- #98 body-measure ↔ training stories (sparse data)
- Full motion system

**P3+**

- #78 public/friends, then #125 signed photo URLs
- Health/Fit, marketplace

**P4**

- #83

---

## GitHub hygiene

Keep using child issues; don’t reopen #57 as a dumping ground.

| If Week 2 is… | Then |
|---------------|------|
| Track A | Open 2 issues: “Rest remaining (lock/shade)” and “Abandoned session ping” |
| Track B | Work #126 |
| Track C | Comment on #99; don’t open a duplicate |

Close children #119–#124 when the matrix row passes, not when the PR merges.

---

## Daily / gym smoke (15 min)

1. Start a template, log a working set + warm-up, rest, background the app → rest-complete fires (dev/prod build, not Android Expo Go)
2. Finish → summary “Next time” still local (no network wait)
3. Settings → Backup & restore → pending goes to 0
4. Optional: second device pull after Week 1 deploy

---

## Agent slices (one per session)

Use these when you want a **small agent** to build, rectify, clean, or lean **without** colliding with Week 1 prove.

**Rules for every slice**

- Take **one** slice. Name it in the first message (`Slice: rest-deeplink`).
- Do not expand into Week 2 Track A Live Activities, #126 volume rewrite, #83, or social.
- Do not rewrite `src/sync/engine.ts` unless the slice is a **matrix bugfix** with a failing case.
- Session logging must never await network / AI / upload.
- Prefer a focused PR. Do not mix docs-only with behavior changes.

**While Week 1 prove is in progress:** prefer *Safe anytime* slices, or *Matrix bugfix* if you have a failing restore.

### Safe anytime (lean / docs / copy)

| ID | Slice | Why | Stay inside |
|----|--------|-----|-------------|
| `docs-schema-drift` | README still says schema v11; bump to 16 and mention photo metadata | Docs lie to the next agent | `README.md` only unless HANDOFF is stale |
| `docs-handoff-date` | Keep HANDOFF branch/schema/next-work true after merge | Agents start here | `docs/HANDOFF.md` |
| `lint-warnings` | Drive down pre-existing lint warnings without behavior change | Lean | One directory per session (`src/lib` or `src/app`) |
| `dead-exports` | Remove unused exports / imports in one module | Lean | One folder; no “while I’m here” refactors |

### Rectify (small product bugs)

| ID | Slice | Why | Stay inside |
|----|--------|-----|-------------|
| `rest-deeplink` | Rest-complete tap opens the live session if one exists, else Home | Rest/reminder currently always go Home | `src/lib/notifications/routes.ts`, `deep-link.ts`, tests |
| `reminder-copy` | Workout reminder body is generic; keep it short, no extra nags | Copy only | `src/lib/notifications/reminders.ts` + tests |
| `ghost-warmups` | Extra test that warm-up sets cannot beat “Last time” volume | Protects #38 | `src/lib/session-ghost.ts` + existing tests |

### Build (only after you pick a Week 2 track)

| ID | Slice | Track | Stay inside |
|----|--------|-------|-------------|
| `abandoned-session` | One local alert ~20–30 min after last set on an open log; cap one per session; tap → that session | A | New notif type + scheduler; Settings opt-in; no set-logging from the shade |
| `rest-lock` | Rest remaining on lock screen (iOS Live Activity) or Android ongoing notification | A | Native rebuild required; gate on Rest timer alerts; Expo 57 `expo-widgets` is iOS-only |
| `volume-definition` | One written rule for stored `total_volume` vs working-set volume; tests; no silent history rewrite | B | #126; Progress / recap / ghost |
| `narrate-live` | Prove Edge Function + stub still safe; no new model keys in the app | C | #99; never call from session complete |

### Matrix bugfix (outranks everything)

If Device B restore fails, open a slice named after the failing row (`matrix-photos`, `matrix-programs`, …). Fix only that path. Re-run the matching matrix row before merging.

### Explicitly not agent slices this window

Home-screen widgets, #94 measurement goals, #112 Google sheet, #78 / #125 signed public photos, #83, chat coach, program generation, logging a set from a notification.
